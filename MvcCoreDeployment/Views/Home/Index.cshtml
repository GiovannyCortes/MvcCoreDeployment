@{
    ViewData["Title"] = "Home Page";
}

<div class="text-center">
    <h1 class="display-4">Deployment CI/CD</h1>
    <h2>@ViewData["SALUDO"]</h2>
    <img src="~/images/m5.jpg" width="250" />
    <ul class="list-group">
        @foreach (int num in Model) {
            <li class="list-group-item">
                @num
            </li>
        }
    </ul>
</div>
